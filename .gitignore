# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.cursor/
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Rust / Cargo（workspace target 在根目录；Cargo.lock 提交，不忽略）
/target

# Python sidecars（各独立 venv，不入库）
.venv/
__pycache__/
*.egg-info/
.pytest_cache/

# git worktrees（git worktree add 在此创建，不入主仓）；.claude/commands 等配置仍入库
.claude/worktrees/

# Miro 板本地镜像（miro-code-spec 从 Miro 板抽取；板=规划真相、可重生覆盖，本地引用即可，不入库）
.miro/

# docs 本地文档与生成产物（PRD / 设计导出等），后续不入库
docs/
volo-dev.pid
.claude/settings.json
.claude/skills/flow-full
.claude/skills/flow-lite
.claude/settings.json
