// @ts-nocheck
/* Vendored Adobe Spectrum 2 design-system components — 1:1 from the Claude
   Design handoff `_ds_bundle.js`. The bundle self-injects each component's CSS
   on first render and publishes the component set to the global namespace
   `window.Spectrum2DesignSystem_b6d1b3`. Importing this module for its side
   effect is enough for the ported pages, which read components off that
   namespace exactly as the prototype did; the named re-exports below are a
   typed-friendly convenience. */
import "./react-global";
import "./vendor/ds_bundle.js";

const DS = (window as any).Spectrum2DesignSystem_b6d1b3 || {};

export const ActionButton = DS.ActionButton;
export const Button = DS.Button;
export const Avatar = DS.Avatar;
export const Card = DS.Card;
export const Tabs = DS.Tabs;
export const Checkbox = DS.Checkbox;
export const RadioGroup = DS.RadioGroup;
export const Radio = DS.Radio;
export const Switch = DS.Switch;
export const TextField = DS.TextField;
export const Badge = DS.Badge;
export const InlineAlert = DS.InlineAlert;
export const Meter = DS.Meter;
export const StatusLight = DS.StatusLight;
export const Tag = DS.Tag;

export default DS;
